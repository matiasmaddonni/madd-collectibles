// Rendered by Next during route transitions on /admin/products
// (pagination, sort, filter change via Link nav). Shows a thin
// progress bar at the top of the viewport.
export default function ProductsLoading() {
  return <div className="ah-progress-bar" aria-hidden />;
}
